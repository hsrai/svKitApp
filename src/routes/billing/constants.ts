/**
 * Billing-specific settings.
 * Only applicable to the /billing route.
 */

export const BILLING_SETTINGS = {
    defaultTaxRate: 0.18, // GST is common at 18% in India
    maxItems: 50,
    dateFormat: 'en-IN' 
};